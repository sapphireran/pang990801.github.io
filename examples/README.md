# Examples

The personal dashboard still lives at [`../index.html`](../index.html).

This folder holds **lab** pages — tools on top of the extracted series — not a second copy of each ECharts option.

- [lab/](lab/README.md) — explorer, regression, story, converters
- Open [lab/index.html](lab/index.html) after `python3 -m http.server` from the repo root
